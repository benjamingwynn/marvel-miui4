echo "backup previous ROM apps for re-use"
mkdir -p MIUI-original
cp -v WORKING_MIUI/system/app/Settings.apk MIUI-original
cp -v WORKING_MIUI/system/app/Camera.apk MIUI-original
cp -v WORKING_MIUI/system/app/LatinIME.apk MIUI-original
echo "clean up old working folders ..."
echo ""
rm -rf CM9-files
rm -rf MIUI-files
mkdir CM9-files
mkdir MIUI-files
echo "Do the following:"
echo "Add to CM9-files: framework-res.apk framework.jar services.jar"
echo "Add to MIUI-files: framework-miui-res.apk framework-res.apk framework.jar services.jar android.policy.jar"
echo "Add to MIUI-files: app folder"
scripts/press_enter
echo "check here if files are added"
MIUI-scripts/framework
MIUI-scripts/services
MIUI-scipts/policy
MIUI-scipts/apks
echo "all patching done, you are ready to cook!"
echo "link now to start building"


